# =============================================================================
# Reproducibility environment for the Nifty VaR research pipeline (Phase 3).
#
# Pin set is a MUTUALLY COMPATIBLE matrix (validated together):
#   - tft_model.py imports `lightning.pytorch` (lightning >= 2.0 namespace)
#   - pytorch_forecasting 1.0.x is the PTF line built on lightning >= 2.1 and
#     returns the 5-field Prediction namedtuple from predict(return_index=True)
#     (the repo's unpack_predictions() handles both old and new forms)
#   - arch 7.x exposes res.convergence_flag (used by build_data/proof tri-state
#     convergence accounting)
#
# Install with:  pip install -r requirements.txt
# (On Google Colab the same matrix is applied so research == reproduction.)
# =============================================================================

# ---- Core scientific stack ------------------------------------------------
numpy>=1.26.4
pandas>=2.2.2
scipy>=1.13.1
matplotlib>=3.8.4
scikit-learn>=1.4.2

# ---- Deep learning (TFT) --------------------------------------------------
torch>=2.2.2
lightning>=2.2.5
pytorch-forecasting>=1.0.0
tensorboard>=2.16.2  # optional; PTF logging backend convenience


# ---- Econometrics ---------------------------------------------------------
arch>=7.0.0
statsmodels>=0.14.2

# ---- Data ingestion -------------------------------------------------------
yfinance>=0.2.41

# ---- Note -----------------------------------------------------------------
# numba is an OPTIONAL pytorch-forecasting accelerator; add `numba==0.59.1`
# only if you accept its LLVM toolchain and version-lock it to numpy<2.
